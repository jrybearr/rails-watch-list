<div class="banner d-flex justify-content-center">
  <h1 class="font">Adding new list</h1>
</div>

<div class="body-background">
  <div class="padding">
    <div class="movie-form p-5">
      <%= simple_form_for [@list] do |f| %>
        <%= f.input :name %>
        <%= f.submit "Add", class: "btn border btn-light" %>
      <% end %>
    </div>
  </div>
</div>
